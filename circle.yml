addons:
  postgresql: "9.3"
before_script:
  - psql -c "create role test with CREATEDB PASSWORD 'test';" -U postgres
  - psql -c 'create database test;' -U test
dependencies:
  pre:
    - pip install -r requirements.txt
test:
  override:
    - ./runtests.sh